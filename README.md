# GitVexdz-Cmake4ish

A CMake-based project repository.

## Overview

This project is set up with Git and CMake build system.

## Getting Started

1. Clone the repository
2. Create a build directory: `mkdir build && cd build`
3. Run CMake: `cmake ..`
4. Build: `cmake --build .`

## License

MIT